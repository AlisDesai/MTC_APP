<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background fill color (optional) -->
    <solid android:color="@android:color/white" /> <!-- You can change this to your desired color -->

    <!-- Stroke (border) -->
    <stroke
        android:color="#fff"
    android:width="4dp"
    />

    <!-- Optional corner radius -->
    <corners android:radius="75dp" /> <!-- To make it circular if needed, adjust the radius to fit your ImageView size -->
</shape>
